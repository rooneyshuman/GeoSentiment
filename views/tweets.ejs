<!DOCTYPE html>
<html>

<head>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <header>
    <% include layouts/header %>
  </header>

  <h1><%= title %></h1>

  <div class="col-sm-12 table-scroll">
    <table class="table table-bordered table-striped table-light">
      <thead>
        <tr>
          <th>Text</th>
          <th>Score</th>
          <th>Positive</th>
          <th>Negative</th>
          <th>Neutral</th>
        </tr>
      </thead>
      <tbody>
        <% for(var i in tweets) { %>
        <tr>
          <td><%=tweets[i].content%></td>
          <% if(tweets[i].score > 0) { %>
            <td style="color: green"><%=tweets[i].score%></td>
          <% } else { %>
            <% if(tweets[i].score == 0) { %>
              <td><%=tweets[i].score%></td>
            <% } else { %>
              <td style="color: red"><%=tweets[i].score%></td>
              <% } %>
            <% } %>
          <td><%=tweets[i].positive%></td>
          <td><%=tweets[i].negative%></td>
          <td><%=tweets[i].neutral%></td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>

</html>